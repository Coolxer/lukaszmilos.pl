---
import { buildSlug } from "~/utils/utils";

export type NavLink = {
  slug: string;
  label: string;
};

export interface Props {
  items?: NavLink[];
  additionalItems?: NavLink[];
  class?: string;
}

const {
  items = [
    {
      slug: "oferta",
      label: "Oferta",
    },
    {
      slug: "portfolio",
      label: "Portfolio",
    },
    {
      slug: "blog",
      label: "Blog",
    },
  ],
  additionalItems = [],
  class: className,
} = Astro.props;

const links = [...items, ...additionalItems];
---

{
  links.map((link) => (
    <a href={buildSlug(link.slug)} class={className}>
      {link.label}
    </a>
  ))
}
