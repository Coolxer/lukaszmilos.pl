---
export interface Props {
  size?: string;
  class?: string;
}

const { size = "max-w-3xl", class: className, ...rest } = Astro.props;
---

<div class:list={["container mx-auto px-[5%]", size, className]} {...rest}>
  <slot />
</div>
